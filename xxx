# Enable debug logging for authorization
kubectl exec -n namespace1 your-inference-pod -c istio-proxy -- curl -X POST localhost:15000/logging?rbac=debug

# View the proxy logs
kubectl logs -n namespace1 your-inference-pod -c istio-proxy --follow
